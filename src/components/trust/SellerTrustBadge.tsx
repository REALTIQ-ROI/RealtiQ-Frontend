import type { TrustBadge } from '../../types/phase45';
const styles: Record<Exclude<TrustBadge, 'none'>, string> = { bronze: 'border-amber-700/30 bg-amber-100 text-amber-900', silver: 'border-slate-400/40 bg-slate-100 text-slate-700', gold: 'border-yellow-500/40 bg-yellow-100 text-yellow-900' };
const SellerTrustBadge = ({ badge, compact = false }: { badge?: TrustBadge | null; compact?: boolean }) => {
  if (badge === undefined || badge === null) return <span className={`inline-flex shrink-0 items-center gap-1 rounded-full border border-outline-variant/30 bg-surface-container-low px-2 py-1 font-bold text-secondary ${compact ? 'text-[10px]' : 'text-xs'}`} aria-label={'RealtIQ trust badge unavailable'} title={'Trust badge information is currently unavailable'}><span className={'material-symbols-outlined text-[1.15em]'} aria-hidden={'true'}>help</span>Badge unavailable</span>;
  if (badge === 'none') return <span className={`inline-flex shrink-0 items-center gap-1 rounded-full border border-outline-variant/30 bg-surface-container-low px-2 py-1 font-bold text-secondary ${compact ? 'text-[10px]' : 'text-xs'}`} aria-label={'No RealtIQ trust badge yet'} title={'This seller has not earned a trust badge yet'}><span className={'material-symbols-outlined text-[1.15em]'} aria-hidden={'true'}>verified</span>No badge yet</span>;
  return <span className={`inline-flex shrink-0 items-center gap-1 rounded-full border font-black capitalize ${styles[badge]} ${compact ? 'px-1.5 py-0.5 text-[10px]' : 'px-2 py-1 text-xs'}`} aria-label={`${badge} RealtIQ trust badge`} title={`${badge[0].toUpperCase()}${badge.slice(1)} RealtIQ trust badge`}><span className={'material-symbols-outlined text-[1.15em]'} aria-hidden={'true'}>verified</span>{compact ? badge : `${badge} badge`}</span>;
};
export default SellerTrustBadge;
