import { render, screen } from '@testing-library/react'; import { MemoryRouter } from 'react-router-dom'; import { describe, expect, it } from 'vitest'; import ValuationResult from './ValuationResult'; import type { AvmValuation } from '../../types/phase45';
const base: AvmValuation = { publicReference: 'RTQ-AVM-1', purpose: 'buyer_research', status: 'completed', asOf: '2026-08-28T10:30:00Z', estimate: 75000000, range: { low: 61500000, high: 88500000 }, currency: 'NGN', confidence: 'medium', comparableCount: 1, sourceMix: { verifiedSales: 0, approvedAsking: 1 }, comparables: [{ publicReference: 'RTQ-PROP-COMP1', sourceType: 'approved_asking', adjustedPrice: 74880000 }] };
describe('ValuationResult', () => { it('labels asking-price evidence and renders returned estimate', () => { render(<MemoryRouter><ValuationResult valuation={base} /></MemoryRouter>); expect(screen.getAllByText(/approved asking/i).length).toBeGreaterThan(0); expect(screen.getByText(/75,000,000/)).toBeInTheDocument(); }); it('never fabricates an estimate for insufficient data', () => { render(<MemoryRouter><ValuationResult valuation={{ ...base, status: 'insufficient_data', confidence: 'insufficient', estimate: undefined, range: undefined, warnings: ['At least 3 eligible comparables are required.'] }} /></MemoryRouter>); expect(screen.getByText('No estimate or range is shown.')).toBeInTheDocument(); expect(screen.queryByText(/75,000,000/)).not.toBeInTheDocument(); expect(screen.getByText(/At least 3/)).toBeInTheDocument(); }); });
